Automaton myCA {
    States := one : white | two : black | three : red

    Neighborhood := (-1,0) | (1,0)

    Transition {
            case {
                #one == cell : { #two }
                #one == #three : { #three }
                otherwise : { #one }
                }

    }

    Default := one
}
