Automaton up {

    States := A : black | B : white

    Neighborhood := (0,1)

    Transition {
        case {
            cell == #B and nei(1) == #A : { #A }
            cell == #A and nei(1) == #A : { #A }
            otherwise : { #B }
            }
    }

    Default := B
}