Automaton myCA {
    States := state1 : white | state2 : black | state3 : red | state4 : gray

    Neighborhood := (1,0) | (0,2) | (0,1)

    Transition {
        if cell == nei(2)
            then #state2
            else #state1
    }

    Default := state1
}
