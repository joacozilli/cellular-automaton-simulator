Automaton myCA {
    States := one : white | two : black | x : red

    Neighborhood := (-1,0) | (1,0)

    Transition {
        let x = neighbors(one)
        in case {
                cell == x && x > 1 : { one }
                otherwise : { x }
                }

    }

    Default := one
}
